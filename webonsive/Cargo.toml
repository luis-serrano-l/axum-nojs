[package]
name = "webonsive"
version = "0.1.0"
edition = "2024"
description = "Zero-JavaScript interactive HTML components for Rust servers"

[features]
default = []
# Axum glue: `Caps` extractor, the `/wo/caps` beacon route, streaming responses.
axum = ["dep:axum"]

[dependencies]
maud.workspace = true
axum = { workspace = true, optional = true }
