[package]
name = "webonsive"
version = "0.1.0"
edition = "2024"
description = "Zero-JavaScript interactive HTML components for Rust servers"

[features]
default = []
# Framework-neutral responses: `prg` as an `http::Response`, `Streamed` as a chunk stream.
http = ["dep:http", "dep:futures-util"]
# Axum glue on top of `http`: extractors, the `/wo/caps` beacon route, `IntoResponse` impls.
axum = ["http", "dep:axum", "dep:bytes"]

[dependencies]
maud.workspace = true
http = { version = "1", optional = true }
axum = { workspace = true, optional = true }
futures-util = { version = "0.3", optional = true, default-features = false, features = ["std"] }
bytes = { version = "1", optional = true }

[dev-dependencies]
axum.workspace = true
tokio.workspace = true
hyper = { version = "1", features = ["server", "http1"] }
hyper-util = { version = "0.1", features = ["tokio"] }
http-body-util = "0.1"

[[example]]
name = "axum_server"
required-features = ["axum"]

[[example]]
name = "hyper_server"
required-features = ["http"]

[package.metadata.docs.rs]
all-features = true
